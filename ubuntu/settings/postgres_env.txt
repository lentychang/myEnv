PGDATABASE=test
PGHOST=127.0.0.1
PGPORT=5432 #(TCP port to connect to; default is 5432)
PGUSER=postgres #(your PostgreSQL user ID; defaults to your login name)
PGPASSWORD=postgres #(your PostgreSQL password, if needed)
